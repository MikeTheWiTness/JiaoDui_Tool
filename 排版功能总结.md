# 校对报告排版功能总结

## 整体架构

```
Word .docx → Pandoc → .md → 拆分题目 → LLM 校对 → 结构化数据 → LaTeX → PDF
```

## PDF 排版特性

**双栏对照布局（paracol）**
- 左栏：原题内容 + 红色上标圈号 ①②③ 标记错误位置
- 右栏：编号修改建议（修改后文字 + 斜体原因说明）
- 多题以 `\newpage` 分页，单文件汇总

**数学公式渲染**
- 行内公式 `$...$` 与行间公式 `$$...$$` 完整保留
- 分数自动升级为 `\dfrac` display-style，清晰可读
- CJK 汉字自动包裹 `\text{}`，确保中文字符在数学模式下正常显示
- LaTeX 命令（`\sin`、`\mathrm`、`\times` 等）完整支持

**化学公式（mhchem）**
- `\ce{H2O}`、`\ce{^{235}_{92}U}` 等化学式原生渲染

**图片处理**
- Markdown 图片转为 `\includegraphics` 嵌入
- 宽度限制为列宽 + keepaspectratio，不溢出
- 图片自动另起一行
- 残留的 Pandoc `{width=...}` 属性文本自动清除

**文本格式**
- Markdown 粗体 `**text**` → `\textbf{}`
- Markdown 斜体 `*text*` → `\textit{}`
- Markdown 标题 `##` → 粗体文字
- 单换行 → `\\` 行分隔

**字体与兼容**
- 中文：宋体（SimSun），英文/数学：Times New Roman
- Pandoc 非标准命令兼容（`\lt`、`\gt`）
- 所有编译通过临时 ASCII 目录，规避中文路径问题

## 校对数据结构

LLM 返回结构化 Markdown，零转义冲突：

```markdown
轻微问题
### 修改 1
- **类型**: text
- **原文**: ``错误文字``
- **改为**: ``正确文字``
- **原因**: 修改原因
```

三种类型：`text`（文字替换）、`rewrite`（整段重写）、`region`（图片/区域问题）
